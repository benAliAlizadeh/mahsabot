# MahsaBot - minimal .htaccess safeguards

Options -Indexes

# Block direct access to sensitive root files
<FilesMatch "^(config\.php|config\.sample\.php|backup\.sh|mahsabot\.sh|README(\-fa)?\.md|LICENSE)$">
    Require all denied
</FilesMatch>

# Protect hidden paths but keep Let's Encrypt challenge reachable
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule (^|/)\.(?!well-known/) - [F,L]
    RewriteRule (^|/)\.git - [F,L]
</IfModule>
